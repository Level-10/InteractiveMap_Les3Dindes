using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Vallon : ZoneParent
{
    // Start is called before the first frame update
    void Start()
    {
        newColor = Color.green;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
