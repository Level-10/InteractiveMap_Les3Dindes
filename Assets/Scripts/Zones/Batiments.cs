using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Batiments : ZoneParent
{
    // Start is called before the first frame update
    void Start()
    {
        newColor = Color.blue;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
