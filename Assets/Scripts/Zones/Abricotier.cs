using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Abricotier : ZoneParent
{
    // Start is called before the first frame update
    void Start()
    {
        newColor = Color.yellow;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
