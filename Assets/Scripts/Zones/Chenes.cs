using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chenes : ZoneParent
{
    // Start is called before the first frame update
    void Start()
    {
        newColor = Color.cyan;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
