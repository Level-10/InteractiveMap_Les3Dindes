using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hollywood : ZoneParent
{
    // Start is called before the first frame update
    void Start()
    {
        newColor = Color.magenta;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
